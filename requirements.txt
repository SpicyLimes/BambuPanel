# BambuPanel dependencies
# Install with: pip install -r requirements.txt --break-system-packages
#
# PyGObject and the Ayatana AppIndicator bindings are NOT installable via pip.
# Install them via apt instead:
#
#   sudo apt install python3-gi python3-gi-cairo \
#                    gir1.2-gtk-3.0 \
#                    gir1.2-ayatana-appindicator3-0.1

paho-mqtt>=1.6.1,<2.0
PyYAML>=6.0
